pipeline {
    agent {
        kubernetes {
            yaml '''
            apiVersion: v1
            kind: Pod
            spec:
                serviceAccount: jenkins-sa
                containers:
                  - name: slave
                    image: docker:latest
                    tty: true
                    securityContext:  # This section grants privileged access
                        privileged: true
                  - name: maven
                    image: maven:alpine
                    command:
                    - cat
                    tty: true
            '''
        }
    }

    stages {
        stage('checkout git') {
            when { // makes it so the jenkins will skip when the changes are not in the app folder
                changeset "app/*"
            }
            steps {
                script {
                    checkout scm
                }
            }
        }

        // Build and push with build tag (replace with actual build commands)
        stage('Build and Push the image with tags') {
            environment {
                auth = 'dockerauth'
            }
            steps { 
                when { // makes it so the jenkins will skip when the changes are not in the app folder
                    changeset "app/*"
                }
                container('slave') {
                    script {
                        def image = docker.build("mikey8520/final-project", "./app")
                        withDockerRegistry(credentialsId: 'dockerauth') {
                            image.push("${env.BUILD_NUMBER}")
                            image.push("latest")
                        }
                    }
                }
            }
        }
    }
    post {
        always {
            echo 'Pipeline POST:'
        }
        success {
            echo 'Pipeline SUCCESS!'
        }
        failure {
            echo 'Pipeline FAILED, check the logs for more information!'
        }
    }
}

